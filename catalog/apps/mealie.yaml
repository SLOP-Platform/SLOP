key: mealie
display_name: Mealie
description: Self-hosted recipe manager with meal planning, shopping lists, and cookbook import
category: productivity
tier: 2
icon: 🍽️
version: "1.0"

image: ghcr.io/mealie-recipes/mealie
image_tag: latest
linuxserver: false
# Opt into the recoverability backup machinery: ms-backup can archive this
# app's config volume, and the agent tracks its backup freshness.
backup_supported: true

ports:
  web: 9000

volumes:
  config: /app/data

dependencies:
  postgres: true

traefik:
  enabled: true
  subdomain: mealie

post_deploy:
  - type: wait_healthy
    timeout: 60
  - type: api_ready
    path: /api/app/about
    timeout: 120

health:
  checks:
    - name: api_reachable
      type: http
      path: /api/app/about
      expect_status: 200
      interval: 60
  self_heal:
    - condition: api_reachable
      action: restart
      max_attempts: 3
      cooldown: 60

tags: [recipes, meal-planning, cooking, productivity]
links:
  docs: https://docs.mealie.io
  github: https://github.com/mealie-recipes/mealie
